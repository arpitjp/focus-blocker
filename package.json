{
  "name": "focus-blocker",
  "version": "1.0.10",
  "private": true,
  "scripts": {
    "preview": "node build.js preview",
    "deploy": "node build.js deploy"
  },
  "devDependencies": {
    "terser": "^5.27.0"
  }
}
